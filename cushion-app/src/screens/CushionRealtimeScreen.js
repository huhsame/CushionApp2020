import React from 'react';
import { View, StyleSheet, Text } from 'react-native';
import { Button } from 'react-native-elements';

const CushionRealtimeScreen = () => {
  return (
    <View>
      <Text>cushion realtime</Text>
    </View>
  );
};

export default CushionRealtimeScreen;
